apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-vroumvroum.fullname" . }}-test-connection"
  labels:
    {{- include "web-vroumvroum.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-vroumvroum.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
