apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-vroumvroum.fullname" . }}
  labels:
    {{- include "web-vroumvroum.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-vroumvroum.selectorLabels" . | nindent 4 }}
